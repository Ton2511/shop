<div class="custom-container-lg mx-auto d-flex flex-column mb-5">
    <div class="mt-5">
        <h4 aria-label="breadcrumb ">
          <ol class="breadcrumb">
            <li class="breadcrumb-item text-dark "><a class="a" href="/">หน้าแรก</a></li>
            <li class="breadcrumb-item text-dark "><a class="a" href="/categories">จัดการหมวดหมู่</a></li>
            
            <li class="breadcrumb-item active" aria-current="page">แก้ไขหมวดหมู่</li>
    
          </ol>
        </h4>
      </div>
    <div class="card mt-3 mb-5 p-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h1 class="text-cyan">แก้ไขหมวดหมู่</h1>
        </div>

        <form action="/categories/edit/<%= category.id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label">ชื่อหมวดหมู่</label>
                <input type="text" name="name" class="form-control" value="<%= category.name %>" required>
            </div>

            <div class="mb-4">
                <label class="form-label">รูปภาพปัจจุบัน</label>
                <div>
                    <% if (category.image) { %>
                        <div class="mb-2">
                            <img src="<%= category.image %>" alt="Category Image" class="img-thumbnail" style="max-width: 200px; max-height: 200px;">
                        </div>
                        <div class="form-check mb-3">
                            <input class="form-check-input" type="checkbox" name="removeImage" id="removeImage" value="yes">
                            <label class="form-check-label text-danger" for="removeImage">
                                ลบรูปภาพนี้
                            </label>
                        </div>
                    <% } else { %>
                        <div class="alert alert-info">
                            <i class="bi bi-info-circle me-2"></i>
                            ไม่มีรูปภาพ
                        </div>
                    <% } %>
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label">อัพโหลดรูปภาพใหม่</label>
                <input type="file" name="image" class="form-control" accept="image/jpeg,image/jpg,image/png,image/gif,image/webp">
                <div class="form-text text-muted">
                    รองรับไฟล์: JPEG, JPG, PNG, GIF, WEBP<br>
                    ขนาดสูงสุด: 5MB
                </div>
            </div>

            <div class="d-flex justify-content-between">
                <button type="submit" class="btn bg-teal text-light">
                    <i class="bi bi-check-circle me-1"></i> บันทึกการเปลี่ยนแปลง
                </button>
                <a href="/categories" class="btn btn-secondary">
                    ยกเลิก
                </a>
            </div>
        </form>
    </div>
</div>